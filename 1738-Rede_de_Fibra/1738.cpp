#include <iostream>
#include <list>

using namespace std;

char Floyd_Warshal(char *adjacencias, int a){
    char D[a][a] = {};
    for (int i = 1; i <= a; i++) {
        for(int j = 1; j <= a; j++) {
            for(int k = 1; k <= a; k++) {

            }
        }
    }
    return D;
}

main() {
    int n, a, b;
    string provedores;
    
    while ((cin >> n) && n > 0) {
        char adjacencias[n + 1][n + 1];
        cin >> a >> b >> provedores;
        
    }
}